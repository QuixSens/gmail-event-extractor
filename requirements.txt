# Google Calendar + Auth libraries
google-api-python-client
google-auth
google-auth-oauthlib

# Date parsing (used by fallback or future-proofing)
python-dateutil

# email is part of the standard library; no need to install via pip
